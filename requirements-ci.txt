# CI dependencies.
# The test suite imports PyTorch directly, so CI needs the CPU wheel.
# Keep the full CUDA/ML stack in requirements.txt for local/model runs.
numpy>=1.26,<3
pytest>=8,<9
Pillow>=10,<12
PyYAML>=6,<7
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.5,<3
