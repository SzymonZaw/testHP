(() => {
  const state={last:null,timer:null};
  const safeJson=value=>{try{return JSON.stringify(value,null,2);}catch{return String(value);}};
  const read=()=>{const node=document.getElementById('spatial-node');const breadcrumbs=[...document.querySelectorAll('#spatial-breadcrumb button')].map(el=>el.textContent.trim()).filter(Boolean);const selected=window.selectedSpatialNode||null,evidence=window.spatialEvidenceTarget||null,manager=window.spatialViewportManager;const managerTarget=manager?.state?.spatial_id||manager?.state?.spatialId||manager?.state?.spatialTarget||manager?.spatialTarget||null;const contract=window.testhpSpatialContract?.getTarget?.()?.spatial_id||window.testhpSpatialContract?.getTarget?.()?.spatialId||window.testhpSpatialContract?.getTarget?.()?.spatial_node_id||null;const bodyTarget=document.body?.dataset?.spatialTarget||null,navId=node?.dataset?.spatialId||node?.getAttribute('data-spatial-id')||null,chosen=managerTarget||contract||selected||evidence||navId||bodyTarget||null,candidates={contract,manager:managerTarget,selectedNode:selected,evidence,navigationNode:navId,body:bodyTarget},distinctIds=[...new Set(Object.values(candidates).filter(Boolean))];return {selected:chosen,navigation:{level:document.getElementById('spatial-level-badge')?.textContent?.trim()||null,label:node?.querySelector('strong')?.textContent?.trim()||null,path:breadcrumbs.join(' > '),childCount:document.querySelectorAll('#spatial-children .spatial-target').length},candidates,canonical:{contract,manager:managerTarget,selectedSpatialNode:selected,evidence,chosen},distinctSpatialIds:distinctIds,mismatch:distinctIds.length>1,warning:distinctIds.length>1?'TARGET CHAIN DRIFT: źródła wskazują różne spatial_id.':null};};
  const fetchJson=async url=>{try{const started=performance.now(),response=await fetch(url,{cache:'no-store'});let payload=null;try{payload=await response.json();}catch{}return {url,status:response.status,ok:response.ok,ms:Math.round(performance.now()-started),payload};}catch(error){return {url,status:0,ok:false,ms:0,error:{name:error?.name||'Error',message:error?.message||String(error)}};}};
  const rowsFrom=payload=>Array.isArray(payload?.items)?payload.items:Array.isArray(payload?.records)?payload.records:Array.isArray(payload?.data)?payload.data:Array.isArray(payload?.evidence)?payload.evidence:Array.isArray(payload?.targetRecords)?payload.targetRecords:[];
  const buildRegistryDiagnostics=(target,registry)=>{const payload=registry.payload||{},rows=rowsFrom(payload),targetId=String(target||'').replace(/^\/+|\/+$/g,''),idOf=x=>String(x?.spatial_node_id||x?.spatial_id||x?.spatialId||x?.target?.spatial_node_id||x?.target?.spatial_id||x?.target?.spatialId||'').replace(/^\/+|\/+$/g,''),targetRecords=rows.filter(x=>idOf(x)===targetId),matchDebug=payload.matchDebug||payload.match_debug||payload.diagnostics||null,prepared=targetRecords.filter(x=>x?.prepared===true||x?.preparedAssetId||x?.prepared_asset_id).length;return {requestedTarget:targetId,endpoint:registry.url,status:registry.status,ok:registry.ok,ms:registry.ms,error:registry.error||null,total:rows.length,targetLinked:targetRecords.length,targetRecords,allRecords:rows,prepared,matchDebug,response:payload};};
  const collectRegistryDiagnostics=async target=>{const targetId=String(target||'').trim().replace(/^\/+|\/+$/g,'');if(!targetId)return null;const encoded=encodeURIComponent(targetId),registry=await fetchJson(`/api/spatial/registry?subject_id=own_cohort&timepoint=T0&spatial_node_id=${encoded}&debug=true`),diagnostics=buildRegistryDiagnostics(targetId,registry);state.last=state.last||{};state.last.registry=diagnostics;window.__testhpTwinRegistryDiagnostics=diagnostics;window.dispatchEvent(new CustomEvent('testhp:evidence-registry-debug',{detail:diagnostics}));return diagnostics;};
  window.__testhpCollectRegistryDiagnostics=collectRegistryDiagnostics;
  const probeRegistry=async target=>{if(!target)return {registry:null,state:null};const encoded=encodeURIComponent(target),[registry,stateResponse]=await Promise.all([fetchJson(`/api/spatial/registry?subject_id=own_cohort&timepoint=T0&spatial_node_id=${encoded}`),fetchJson(`/api/spatial/state?subject_id=own_cohort&timepoint=T0&spatial_node_id=${encoded}`)]),registryRows=rowsFrom(registry.payload),registryIds=registryRows.map(item=>item.spatial_node_id||item.spatial_id).filter(Boolean),stateSpatialId=stateResponse.payload?.spatial_id||stateResponse.payload?.item?.spatial_id||null;return {registry:{target,http:{status:registry.status,ok:registry.ok,ms:registry.ms},recordCount:registryRows.length,targetLinked:registryRows.filter(item=>(item.spatial_node_id||item.spatial_id)===target).length,spatialIds:registryIds,response:registry.payload},state:{url:stateResponse.url,spatial_id:stateSpatialId,response:stateResponse.status,payload:stateResponse.payload}};};
  const render=(chain,probe)=>{const panel=document.getElementById('twin-boot-diagnostics');if(!panel)return;let block=document.getElementById('testhp-target-chain-debug');if(!block){block=document.createElement('details');block.id='testhp-target-chain-debug';block.className='twin-debug-section';panel.appendChild(block);}block.open=true;block.innerHTML=`<summary style="cursor:pointer;font-weight:800">TARGET CHAIN / PROVENANCE · ${chain.mismatch?'TARGET DRIFT':'TARGET CHAIN OK'}</summary><pre style="white-space:pre-wrap;word-break:break-word;margin:8px 0 0">${safeJson(chain)}\n\nREGISTRY / CACHE PROBE\n${safeJson(probe)}</pre>`;};
  const run=async()=>{const chain=read(),probe=await probeRegistry(chain.selected);state.last={at:new Date().toISOString(),chain,probe};window.__testhpTargetChainDiagnostics=state.last;window.dispatchEvent(new CustomEvent('testhp:target-chain-diagnostics',{detail:state.last}));render(chain,probe);await collectRegistryDiagnostics(chain.selected);return state.last;};
  const install=()=>{if(state.timer)return;run();state.timer=setInterval(run,1500);window.addEventListener('testhp:spatial-target-changed',run);window.addEventListener('testhp:spatial-layer-changed',run);window.addEventListener('beforeunload',()=>clearInterval(state.timer),{once:true});};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
})();
