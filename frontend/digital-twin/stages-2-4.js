(() => {
  const root=document.querySelector('.app-shell'); if(!root)return;
  const subjectId='own_cohort',timepoint='T0',STORAGE='digitalTwinEvidenceUX.v2';
  const $=id=>document.getElementById(id),wait=ms=>new Promise(r=>setTimeout(r,ms));
  const tf=(url,options={},ms=8000)=>{const c=new AbortController(),t=setTimeout(()=>c.abort(),ms);return fetch(url,{...options,signal:c.signal}).finally(()=>clearTimeout(t));};
  const slug=v=>String(v||'').toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');
  const childId=(p,l)=>{
    const k=String(l||'').toLowerCase().trim();
    const d={palm:'palm','śródręcze':'palm',thumb:'thumb','kciuk':'thumb',index:'index','palec wskazujący':'index',middle:'middle','palec środkowy':'middle',ring:'ring','palec serdeczny':'ring',little:'little','mały palec':'little',wrist:'wrist','nadgarstek':'wrist',thenar:'thenar','kłąb kciuka':'thenar',hypothenar:'hypothenar','kłębik dłoni':'hypothenar','central palm':'central-palm','centralna część dłoni':'central-palm'};
    if(d[k])return d[k];
    if(k==='proximal segment'||k==='odcinek bliższy')return`${p}-proximal`;
    if(k==='middle segment'||k==='odcinek środkowy')return`${p}-middle`;
    if(k==='distal segment'||k==='odcinek dalszy')return`${p}-distal`;
    const f=k.match(/(?:microscopy field|pole mikroskopowe)\s*([abc])/i);if(f)return`${p}-field-${f[1].toLowerCase()}`;
    const c=k.match(/(?:cell target|cel komórkowy)\s*(\d+)/i);if(c)return`${p}-cell-${c[1]}`;
    return slug(l);
  };
  const labels=()=>[...document.querySelectorAll('#spatial-breadcrumb button')].map(b=>b.textContent.trim()).filter(Boolean);
  const nodeId=()=>{const ls=labels();if(!ls.length)return'hand';const ids=['hand'];ls.slice(1).forEach(l=>ids.push(childId(ids.at(-1),l)));return ids.join('/');};
  const sync=()=>{const id=nodeId(),ls=labels(),label=ls.length?ls.join(' > '):'Dłoń';window.spatialEvidenceTarget=id;window.selectedSpatialNode=id;document.body.dataset.spatialTarget=id;const t=$('evidence-target-label');if(t)t.textContent=` · ${label}`;window.dispatchEvent(new CustomEvent('testhp:spatial-target-changed',{detail:{spatial_target_id:id,label}}));return id;};
  const ensureTarget=()=>{const bc=$('spatial-breadcrumb');if(!bc)return false;const ls=labels();if(!ls.length)return false;if(ls.length===1&&(ls[0].toLowerCase()==='hand'||ls[0].toLowerCase()==='dłoń')){const palm=[...document.querySelectorAll('#spatial-children button')].find(b=>{const t=b.textContent.trim().toLowerCase();return t.startsWith('palm')||t.startsWith('śródręcze');});if(palm){palm.click();return true;}return false;}sync();return true;};
  const hideDebug=()=>document.querySelectorAll('*').forEach(el=>{if(el.children.length>8)return;const t=(el.textContent||'').trim();if(/^TWIN-VIEWPORT DEBUG/i.test(t)||/^TWIN-VIEWPORT DEBUGCLEAR/i.test(t)||/^DEBUG$/i.test(t)){el.style.display='none';el.setAttribute('aria-hidden','true');}});
  const clean=()=>document.querySelectorAll('button').forEach(b=>{if(/^\s*[＋+]\s*Add observation\s*$/i.test((b.textContent||'').trim())){if(b.closest('#evidence-workspace'))b.textContent='＋ Add biological observation';else b.remove();}});
  const normalizeImported=(x,target='hand')=>({id:`backend-${x.asset_id}`,backendAssetId:x.asset_id,type:'Macro',sourceType:'upload',target:x.spatial_target_id||target,timepoint:x.timepoint||timepoint,date:x.date||new Date().toISOString().slice(0,10),modality:x.modality||'Hand image',resolution:x.resolution||'',subject:x.subject_id||subjectId,operator:x.operator||'',filename:x.filename||x.view||`hand-${x.asset_id}`,fileData:'',signals:Array.isArray(x.signals)?x.signals:[],annotations:'',comments:'Imported from registered hand evidence.',history:[{at:new Date().toISOString(),action:'imported from registry'}],archived:false});
  const install=()=>{if($('stage24-hierarchy'))return;const p=document.createElement('section');p.className='panel';p.innerHTML='<div class="panel-title"><div><span class="section-kicker">PODSUMOWANIE HIERARCHICZNE</span><strong>MAKRO → TKANKA → KOMÓRKOWE → KOMÓRKA</strong></div><span class="research-badge">ETAP 4</span></div><div id="stage24-hierarchy" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:12px"></div><p class="muted" style="margin-top:12px">Podsumowania nadrzędne agregują wyłącznie dane jawnie przypisane do elementów potomnych. Nigdy nie tworzą danych, których nie ma.</p>';document.querySelector('.state-panel')?.after(p);};
  const render=nodes=>{const c=$('stage24-hierarchy');if(!c)return;c.replaceChildren();nodes.forEach(n=>{const x=document.createElement('div');x.className='state-card';const age=n.biological_age?.overall;x.innerHTML=`<span>${n.node_id}</span><strong>${age==null?'Nieustalone':`${age} · wskaźnik`}</strong><small>${n.status==='observed'?'Dane obserwowane':'Niewystarczające dane'} · ${n.evidence_count||0} ${n.evidence_count===1?'element danych':'elementów danych'}</small>`;c.appendChild(x);});if(!nodes.length)c.innerHTML='<div class="state-card"><span>Hierarchia</span><strong>Nieustalone</strong><small>Przypisz dane, aby rozpocząć agregację.</small></div>';};
  const refresh=async()=>{const id=sync();try{const[a,b]=await Promise.all([tf(`/api/spatial/state?subject_id=${encodeURIComponent(subjectId)}&timepoint=${encodeURIComponent(timepoint)}&spatial_node_id=${encodeURIComponent(id)}`),tf('/api/spatial/summary',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({subject_id:subjectId,timepoint,root_node_id:id})})]);if(!a.ok||!b.ok)return;const st=await a.json(),su=await b.json(),age=st.biological_age?.overall,q=st.signals||{},count=Number(st.evidence_count||0);if($('age-state'))$('age-state').textContent=age==null?'Nieustalone':`${age} · wskaźnik badawczy`;const set=(id,v)=>{const e=$(id);if(e)e.textContent=v==null?'Nieustalone':`${v} · obserwowany sygnał`;};set('structure-state',q.elasticity?.value??q.collagen_structure?.value??q.health_score?.value);set('damage-state',q.stress_score?.value??q.fibrosis?.value);set('pathology-state',q.inflammation?.value);if($('evidence-count'))$('evidence-count').textContent=`${count} ${count===1?'element':'elementów'}`;if($('confidence-state'))$('confidence-state').textContent='Nieustalone';if($('evidence-level'))$('evidence-level').textContent=count?'Jawnie przypisane dane badawcze':'Niewystarczające dane';render(su.nodes||[]);updateInspectorEvidence(id,count);}catch(err){console.warn('Odświeżanie etapów 2–4 nie powiodło się',err);}};
  const updateInspectorEvidence=(id,count)=>{const label=id==='hand'?'Dłoń':id.split('/').pop().replace(/[-_]+/g,' ').replace(/\b\w/g,c=>c.toUpperCase());let evidence=[];try{const x=JSON.parse(localStorage.getItem(STORAGE)||'{}');evidence=Array.isArray(x.evidence)?x.evidence:[]}catch{}const local=evidence.filter(e=>(e.target||'hand')===id&&!e.archived).length;const rootCount=evidence.filter(e=>(e.target||'hand')==='hand'&&!e.archived).length;const parent=id==='hand'?0:rootCount;const detail=$('macro-detail');if(detail&&id!=='hand'){detail.textContent=local?`${local} dane makro przypisane bezpośrednio do ${label}.`:parent?`${parent} obserwacji makro zarejestrowanych na poziomie Dłoni; żadna nie jest przypisana przestrzennie do ${label}.`:'Brak danych makro zarejestrowanych dla tego regionu.';}};
  const seed=async()=>{try{const r=await tf(`/api/hand/analysis?subject_id=${encodeURIComponent(subjectId)}&timepoint=${encodeURIComponent(timepoint)}`);if(!r.ok)return 0;const a=await r.json(),assets=(a.assets||[]).filter(x=>['ready','available'].includes(String(x.status||'').toLowerCase())),hands=assets.filter(x=>String(x.modality||'').toLowerCase()==='hand');let s={};try{s=JSON.parse(localStorage.getItem(STORAGE)||'{}')}catch{}const old=Array.isArray(s.evidence)?s.evidence:[],map=new Map(old.map(x=>[x.backendAssetId||x.id,x]));hands.forEach(x=>{const prev=map.get(x.asset_id);map.set(x.asset_id,prev?{...normalizeImported(x,prev.target||x.spatial_target_id||'hand'),...prev,backendAssetId:x.asset_id}:{...normalizeImported(x,x.spatial_target_id||'hand')});});const evidence=[...map.values()];localStorage.setItem(STORAGE,JSON.stringify({evidence,target:nodeId()}));window.dispatchEvent(new CustomEvent('testhp:evidence-registry-synced',{detail:{count:hands.length,evidence}}));return hands.length;}catch(err){console.warn('Synchronizacja rejestru danych nie powiodła się',err);return 0;}};
  const boot=async()=>{install();hideDebug();let ok=false;for(let i=0;i<40&&!ok;i++){ok=ensureTarget();if(!ok)await wait(100);}if(!ok)sync();const count=await seed();clean();await refresh();const s=$('twin-status');if(s)s.textContent=count?`${count} obserwacji makro załadowano`:'Cyfrowy bliźniak gotowy';hideDebug();clean();};
  const bc=$('spatial-breadcrumb');if(bc)new MutationObserver(()=>{hideDebug();clean();if(labels().length){sync();refresh();}}).observe(bc,{childList:true,subtree:true});new MutationObserver(()=>{hideDebug();clean();}).observe(root,{childList:true,subtree:true});window.addEventListener('testhp:evidence-attached',refresh);window.addEventListener('testhp:evidence-registry-synced',()=>{try{const x=JSON.parse(localStorage.getItem(STORAGE)||'{}');if(Array.isArray(x.evidence))window.dispatchEvent(new CustomEvent('testhp:evidence-attached'));}catch{}});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
