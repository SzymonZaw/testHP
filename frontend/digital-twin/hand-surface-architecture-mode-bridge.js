(() => {
  'use strict';

  // Legacy URL compatibility only. Architecture mode is now owned by the
  // canonical geometry/data modules. Do not add synchronization logic here.
  window.testhpHandSurfaceArchitectureModeBridge = window.testhpHandSurfaceArchitectureModeBridge || {};
})();
