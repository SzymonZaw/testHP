# Phase A debug notes

The TWIN VIEWPORT debug output currently shows a canonical `hand/palm` spatial ID but a legacy `Palm` manager/navigation target and zero target-linked evidence. This file records the observed state while implementation is verified.

Expected canonical state: `Śródręcze` / `hand/palm`.
