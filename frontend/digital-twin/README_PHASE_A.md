# Phase A implementation boundary

The canonical region contract is `hand/palm` with UI label `Śródręcze`.

The viewport manager and navigation layer must emit the canonical spatial ID rather than relying on a later normalization pass. Evidence records must carry a target that can be resolved to the same canonical spatial ID.
