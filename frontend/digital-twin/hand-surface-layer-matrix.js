(() => {
  'use strict';
  window.testhpLayerMatrix = {
    macro: ['photo','measurement','observation'],
    tissue: ['photo','histology','observation'],
    cellular: ['microscopy','marker','observation'],
    cell: ['molecular','expression','observation']
  };
})();